class Configs:
    DATA_ROOT = "../data"
    MODEL_ROOT = "../models"
    RESULT_ROOT = "../results"
    TARGET_DATASET_NAMES = ["bottle", "cable"]
    IMAGE_SIZE = 256
    BATCH_SIZE = 2
    BACKBONE_NAME = "wideresnet50"
    DEVICE = "cuda"

